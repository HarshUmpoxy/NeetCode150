# Simple two sum as the water content depends on the minimum height,
# By moving the pointer with smaller height, we can try to find a taller height
# Also, even if we move the other pointer, we still shrink the breadth
# So, it's better to move the pointer with smaller height